global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: mobility-pulse
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - host.docker.internal:8080
